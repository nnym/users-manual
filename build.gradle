plugins {
    id("java-library")
    id("maven-publish")
    id("net.auoeke.gronk").version("4.+")
}

version("0.1.0")
javaVersion(17)

repositories {
    maven {url = "https://maven.auoeke.net"}
}

dependencies {
    api("net.auoeke:reflect", "net.auoeke:romeral", "net.auoeke:unsafe")
}
